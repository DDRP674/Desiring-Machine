# DM_v1.1_discarded

This is a discarded version. I think there's no big difference from the previous version, except maybe this version implemented the harddisk storage of the tree, so that it does not need to be regenerated everytime.  